//Area of Rectangle (Using hard coding inputs)

#include <stdio.h>

int main(){
    float length = 8.33;
    float breadth = 6.45;
    printf("The area of rectangle %f",length*breadth);
    return 0;
}